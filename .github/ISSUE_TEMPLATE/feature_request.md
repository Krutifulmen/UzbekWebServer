---
name: "✨ Предложить улучшение"
description: "Предложить новую функцию или улучшение существующей"
title: "[FEATURE] "
labels: ["enhancement", "triage"]
assignees: []
body:
  - type: markdown
    attributes:
      value: |
        ## ✨ Запрос на новую функцию
        Спасибо за интерес к улучшению Uzbek Web Server! 🇺🇿
        Пожалуйста, опишите ваше предложение как можно подробнее.
  - type: checkboxes
    id: prerequisites
    attributes:
      label: "Проверка перед отправкой"
      description: "Пожалуйста, подтвердите следующее"
      options:
        - label: "Я проверил документацию в DOCS/doc.md"
          required: true
        - label: "Я проверил существующие issues, чтобы убедиться, что это новое предложение"
          required: true
        - label: "Это улучшение соответствует философии проекта (минимализм, простота)"
          required: true
  - type: dropdown
    id: type
    attributes:
      label: "Тип улучшения"
      description: "Что вы предлагаете?"
      options:
        - "Новая функция"
        - "Улучшение существующей функции"
        - "Улучшение производительности"
        - "Улучшение безопасности"
        - "Улучшение пользовательского опыта"
        - "Улучшение документации"
        - "Другое"
      default: 0
    validations:
      required: true
  - type: textarea
    id: problem
    attributes:
      label: "Проблема или возможность"
      description: |
        Опишите проблему, которую решает это улучшение, или возможность, которую оно добавляет.
        Например: "Мне неудобно каждый раз указывать порт..." или "Было бы полезно, если бы сервер..."
      placeholder: |
        Сейчас при запуске сервера нужно каждый раз указывать порт и директорию.
        Было бы удобно иметь конфигурационный файл с настройками по умолчанию.
    validations:
      required: true
  - type: textarea
    id: solution
    attributes:
      label: "Предлагаемое решение"
      description: "Опишите, как вы видите реализацию этой функции"
      placeholder: |
        Я предлагаю добавить поддержку конфигурационного файла ~/.uzws/config, в котором можно указать:
        - Порт по умолчанию
        - Директорию по умолчанию
        - Другие настройки
        
        Сервер будет сначала проверять аргументы командной строки, затем конфиг, затем значения по умолчанию.
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: "Альтернативные решения"
      description: "Рассматривали ли вы альтернативные решения? Какие есть варианты?"
      placeholder: |
        Альтернатива 1: Использовать переменные окружения (UZWS_PORT, UZWS_DIR)
        Альтернатива 2: Добавить флаги командной строки для всех настроек
        Альтернатива 3: Создать отдельный скрипт конфигурации
  - type: textarea
    id: benefits
    attributes:
      label: "Преимущества"
      description: "Какие преимущества принесет это улучшение?"
      placeholder: |
        1. Упростит запуск сервера для регулярных задач
        2. Снизит количество опечаток при вводе параметров
        3. Позволит легко переключаться между разными проектами
        4. Улучшит пользовательский опыт
  - type: textarea
    id: drawbacks
    attributes:
      label: "Возможные недостатки"
      description: "Есть ли потенциальные недостатки или побочные эффекты?"
      placeholder: |
        1. Усложнит код (добавится парсинг конфигурационного файла)
        2. Может вызвать путаницу с приоритетом настроек (аргументы vs конфиг vs env)
        3. Потребует обновления документации
  - type: textarea
    id: implementation
    attributes:
      label: "Идеи по реализации"
      description: |
        Если у вас есть технические идеи, как это реализовать, поделитесь ими.
        Это не обязательно, но поможет быстрее разработать функцию.
      placeholder: |
        Можно добавить функцию load_config() в начало скрипта, которая будет:
        1. Проверять наличие ~/.uzws/config
        2. Читать файл построчно
        3. Парсить ключ=значение
        4. Устанавливать переменные
        
        Пример конфига:
        PORT=8080
        DIRECTORY=./public
        ENABLE_LOGGING=true
  - type: textarea
    id: examples
    attributes:
      label: "Примеры использования"
      description: "Приведите примеры, как будет использоваться новая функция"
      placeholder: |
        Пользователь создает конфиг:
        ```
        echo "PORT=3000" > ~/.uzws/config
        echo "DIRECTORY=~/projects/my-site" >> ~/.uzws/config
        ```
        
        Затем просто запускает:
        ```
        uzws
        ```
        И сервер автоматически запускается на порту 3000 из указанной директории.
  - type: dropdown
    id: priority
    attributes:
      label: "Приоритет с вашей точки зрения"
      description: "Насколько важно это улучшение?"
      options:
        - "Критически важно - без этого сложно использовать проект"
        - "Высокий - значительно улучшит пользовательский опыт"
        - "Средний - было бы неплохо иметь"
        - "Низкий - косметическое улучшение"
        - "Не уверен"
      default: 1
  - type: textarea
    id: related
    attributes:
      label: "Связанные функции или issues"
      description: "Связано ли это предложение с другими функциями или существующими issues?"
      placeholder: |
        Связано с issue #42 (Добавление поддержки конфигурации)
        Дополняет функцию логирования из версии 1.5
  - type: checkboxes
    id: contribution
    attributes:
      label: "Готовы ли вы помочь?"
      description: "Будете ли вы участвовать в разработке этой функции?"
      options:
        - label: "Да, я готов помочь с реализацией"
        - label: "Да, я могу помочь с тестированием"
        - label: "Да, я могу помочь с документацией"
        - label: "Нет, я только предлагаю идею"
  - type: textarea
    id: additional
    attributes:
      label: "Дополнительная информация"
      description: "Любая другая информация, которая может быть полезна"
      placeholder: |
        Я использую Uzbek Web Server для обучения студентов веб-разработке.
        Эта функция помогла бы мне быстрее переключаться между демонстрационными проектами.
